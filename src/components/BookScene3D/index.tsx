/**
 * BookScene3D — point d'entrée lazy-loadé
 *
 * Ce fichier est importé via React.lazy() dans StoryLibrary,
 * ce qui garantit que Three.js ne charge que lors de l'animation.
 */
export { BookScene3D as default } from './BookScene3D';
